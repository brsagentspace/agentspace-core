# AgentSpace brand — "Command Orbit" (2026-08-22)

- `agentspace-mark.svg` — mark (currentColor; UI'da `src/components/brand/BrandMark.tsx` aynı geometri)
- `agentspace-lockup.svg` — mark + wordmark + tagline
- `agentspace-app-icon.svg` — 512 koyu zemin + halo; `src-tauri/icons/*` bu ikondan `tauri icon` ile üretildi (1024 PNG kaynağı `design/brand/`)
- Alternatif yönler + sunum sayfası: `design/brand/` (paketlenmez)
